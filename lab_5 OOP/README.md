#  Звіт про Виконання Лабораторної Роботи №5
## Тема: Знайомство з ООП

## Мета роботи: 
Навчитись використовувати основні принципи ООП, розглянути кострукції побудови класу та створення обєктів та навчитись працювати з ними

### 🧑‍🎓 Виконав:
* **Ім'я:** Віталій Глод
* **Група:** КН-32
* **Дата:** 14.12.2025

## 💻 Виконання Роботи
Практична робота з ООП (Клас MyName)

| Етап | Результати виконання |
| :--- | :--- |
| **Модифікація конструктора** | Додано `capitalize()` та перевірку `isalpha()`. Тепер ім'я завжди починається з великої літери, а цифри викликають помилку. |
| **Методи та властивості** | Створено властивість `full_name` та додано метод `count_letters()` для підрахунку довжини імені. |
| **Робота з файлами** | Реалізовано метод `save_to_file()`, який записує дані користувачів у файл `users.txt`. |
| **Статичні та Клас-методи** | Використано `@classmethod` для створення анонімних користувачів та `@staticmethod` для привітань. |

### Програма вивела значення (Результат ООП):
Об'єкт: User #1: Bohdan (bohdan@itcollege.lviv.ua)
Кількість літер: 6
You say: Привіт, світе!
Об'єкт: User #2: Marta (marta@itcollege.lviv.ua)        
Кількість літер: 5
You say: Привіт, світе!
Об'єкт: User #3: Vitalik (vitalik@itcollege.lviv.ua)    
Кількість літер: 7
You say: Привіт, світе!
Об'єкт: User #5: Anonymous (anonymous@itcollege.lviv.ua)
Кількість літер: 9
You say: Привіт, світе!

Кількість імен у списку: 4
Кількість створених об'єктів: 5

### Відповіді на індивідуальні завдання
1. Чому коли передаємо значення None створюється об'єкт з іменем "Anonymous"? Це відбувається завдяки логіці в конструкторі __init__. Вираз self.name = name if name is not None else self.anonymous_user().name перевіряє вхідне значення. Якщо воно відсутнє (None), програма звертається до методу класу anonymous_user(), який автоматично створює екземпляр з ім'ям "Anonymous".
2. Як змінити текст привітання при виклику методу say_hello()? Статичний метод say_hello має параметр за замовчуванням. Щоб змінити текст, потрібно передати свій рядок як аргумент під час виклику методу
3. Чому кількість імен у списку names та результат total_names відрізняються? Це стається через "приховану" ініціалізацію. Коли ми обробляємо None, конструктор викликає self.anonymous_user(). Цей метод створює ще один (тимчасовий) об'єкт класу всередині конструктора. Оскільки лічильник total_names += 1 спрацьовує при кожній ініціалізації, цей тимчасовий об'єкт також враховується, що збільшує загальну суму на одиницю більше, ніж ми очікували.

### Висновок
Що зроблено: Налаштовано VS Code, освоєно роботу з Jupyter Notebook та розроблено повноцінний клас на Python з використанням принципів ООП.

Досягнення мети: Мета досягнута повністю. Отримано навички створення методів, властивостей та валідації даних.

Складнощі:-